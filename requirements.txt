# ─────────────────────────────────────────────────────────────
# ASR Divine — Python Developer Tooling
# Virtualenv: asrdivine
# Activate:   asrdivine\Scripts\Activate.ps1  (PowerShell)
#             asrdivine\Scripts\activate.bat   (CMD)
#             source asrdivine/Scripts/activate (Git Bash)
# Install:    pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# HTTP client — for API health checks and integration scripts
httpx==0.28.1

# Fake data generation — for database seeding scripts
Faker==40.15.0

# .env file loader — reads .env.local for scripts
python-dotenv==1.2.2

# Testing framework
pytest==9.0.3
pytest-asyncio==1.3.0

# Beautiful terminal output for CLI scripts
rich==14.3.4

# CLI argument parsing — for building seed/export scripts
typer==0.25.1

# CSV and data processing
pandas==3.0.3

# Supabase Python client — direct DB access from scripts
supabase==2.30.0

# HTTP requests (used by supabase client internally)
requests==2.34.1

# Date/time utilities
python-dateutil==2.9.0.post0

# ── Pinned transitive deps (locked for reproducibility) ───────

# Supabase sub-clients
postgrest==2.30.0
realtime==2.30.0
storage3==2.30.0
supabase-auth==2.30.0
supabase-functions==2.30.0

# HTTP / async core
anyio==4.13.0
httpcore==1.0.9
h11==0.16.0
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0

# Auth / crypto
cryptography==48.0.0
PyJWT==2.12.1
cffi==2.0.0
pycparser==3.0

# Pydantic (used by supabase client)
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0

# Async / network utilities
websockets==15.0.1
tenacity==9.1.4
yarl==1.23.0
multidict==6.7.1
propcache==0.5.2

# Data / pandas deps
numpy==2.4.4
tzdata==2026.2
python-dateutil==2.9.0.post0
six==1.17.0

# Misc utilities
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.15
urllib3==2.7.0
packaging==26.2
colorama==0.4.6
click==8.3.3
shellingham==1.5.4
Pygments==2.20.0
markdown-it-py==4.2.0
mdurl==0.1.2

# pytest plugins / infra
pluggy==1.6.0
iniconfig==2.3.0

# supabase extras
deprecation==2.1.0
cachetools==6.2.6
fsspec==2026.4.0
mmh3==5.2.1
pyroaring==1.1.0
pyiceberg==0.11.1
zstandard==0.25.0
strictyaml==1.7.3
StrEnum==0.4.15
pyparsing==3.3.2
annotated-doc==0.0.4
